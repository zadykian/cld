# cld

Run [Claude Code](https://code.claude.com) in named sessions on a private tmux server:
attach, detach and reattach from any terminal without losing the conversation.

```
cld          # attach to (or create) the session "cld-main"
cld review   # attach to (or create) the session "cld-review"
```

Work in progress.
